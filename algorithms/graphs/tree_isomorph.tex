\subsection{Tree Isomorph}

Checks whether two tree are isomorph. 
The function thash() returns the hash of the tree (using centroids as special vertices).
Two trees are isomorph if their hash are the same.